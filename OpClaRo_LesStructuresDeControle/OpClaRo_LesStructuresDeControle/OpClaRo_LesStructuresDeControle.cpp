// OpClaRo_LesStructuresDeControle.cpp 

#include <iostream>

using namespace std;

	int main()
	{
		// Test 1 avec la condition If-Else
		/*
		double moyenne;

		cout << "Dis moi quelle est ta moyenne jeune homme ?" << endl;
		cin >> moyenne;

			if (moyenne > 0 && moyenne < 10)
			{
				cout << "Bah alors mon vieux il faut te ressaisir !" << endl;
			}
			else if (moyenne >= 10 && moyenne < 14)
			{
				cout << "Tu peux encore mieux faire j'en suis sur !" << endl;
			}
			else if (moyenne >= 14 && moyenne < 18)
			{
				cout << "C'est bien continue comme  ca !" << endl;
			}
			else if (moyenne >= 18 && moyenne <= 20)
			{
				cout << "Magnifique que demande le peuple ! " << endl;
			}
			else
			{
				cout << "Mon pote arrete l'ecole c'est mieux pour toi" << endl;
			}
		return 0;
		
		
		// Test 2 avec la fonction switch

		int nbEnfants;

		cout << "Indiquez le nombre d'enfants que vouz avez :" << endl;
		cin >> nbEnfants;

		switch (nbEnfants)
		{
			case 0:
				cout << "Et bah alors il faut vous bougez allez hop aller faire tiki-taka !!" << endl;
				break;

			case 1:
				cout << "C'est pour quand le deuxieme ?" << endl;
				break;

			case 2:
				cout << "Waw qu'ils sont beau !" << endl;
				break;

			default:
				cout << "Faut arreter des faire des gosses la !" << endl;
					break;
		}
		return 0;
		

		// Test 3 avec les variables booléenes

		bool noir(false);

		if (noir)  // synonyme de if (noir == true) 
		{
			cout << "La couleur est bien noire" << endl;
		}
		else
		{
			cout << "La couleur n'est pas noire" << endl;
		}
		return 0;
		

		// Test 4 avec la boucle while/do-while

		int nbEnfants(0);

		while (nbEnfants < 3)
		{
			cout << "Combien d'enfants avez vous ?" << endl;
			cin >> nbEnfants;

			switch (nbEnfants)
			{
				case 0:
					cout << "Revenez quand vous en aurez fait 3 ou plus" << endl;
					break;
				case 1:
					cout << "Un peu d'effort encore 2" << endl;
					break;
				case 2:
					cout << "Vous y etes presques plus que un !" << endl;
					break;
			}
		}

		cout << "Bravo vous avez au minimum 3 enfants vous avez droit aux allocations" << endl;
		return 0;
		

		// Test 5 la boucle for
		
		int nombre;

		for (nombre = 0; nombre < 10; nombre++)
		{
			cout << nombre << " " ;
		}
		return 0;
		

		// Test 6 Variantes de for

		for (int a(0); a < 10; a += 2) // on peut initialiser une variables dans la boucle plus on peut incrémenter autre chose que par 1 ou decrementer
		{
			cout << a << " ";
		}
		return 0;
		*/
	}

